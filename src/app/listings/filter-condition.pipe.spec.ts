import { FilterConditionPipe } from './filter-condition.pipe';

describe('FilterConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
